---
description: Derive test cases with named design techniques (EP, BVA, decision table, state transition, pairwise, error guessing) and build the traceability matrix.
argument-hint: [requirement IDs to focus on] [--gherkin]
---

# /qz-design-tests

Use the `qz-test-designer` agent. Focus / options: `$ARGUMENTS`

Write `qa/03-design/test-cases.md` and `qa/03-design/rtm.md` (plus
`qa/03-design/features/*.feature` with `--gherkin`). Report case counts by
technique and any requirement without coverage.
